{
  // Neolink.NET configuration.
  // Copy this file to config.json and edit it — comments and trailing commas are allowed.

  // Address to serve on. "0.0.0.0" = all networks, "127.0.0.1" = this computer only.
  "bind": "0.0.0.0",

  // RTSP port (for VLC, ffmpeg, Frigate, Blue Iris, Home Assistant, ...).
  "bind_port": 8654,

  // HTTP/WebSocket port: the web UI and the JSON/fMP4 API. 0 disables both.
  "web_port": 8655,

  // Serve the browser UI on web_port. Set false for API-only / headless setups.
  "webui": true,

  // Web-UI specific settings. NOTE: UI sign-in accounts are NOT configured here —
  // they live in users.json (created from the UI itself) and are entirely separate
  // from the RTSP "users" list below.
  "ui": {
    // Where the UI's server-side state persists: users.json (accounts) and
    // settings.json (per-user layouts, filters, recording switches).
    // Defaults to this config file's directory — point it at a persistent volume
    // if your config is mounted read-only or replaced on deployments, otherwise
    // accounts and layouts are lost every deploy.
    // "state_dir": "/config",

    // Sign-in recovery: while true, the login screen allows setting a new admin
    // password WITHOUT the old one. Set it back to false right after use!
    "reset_admin_password": false,

    // Playback speed of the ambient clip previews in the review strip.
    "trickle_speed": 4,

    // Two-way talk — a mic button on talk-capable cameras (doorbells etc.)
    // streams your microphone to the camera's speaker. Off by default; can also
    // be toggled from the web UI (Server settings → Web UI). Browsers only allow
    // microphone access on HTTPS or localhost pages.
    "talk": false,

    // Show the admin-only sidebar strip that reports live server work (footage
    // archiving progress, and future long-running jobs). On by default; set false
    // to hide it for everyone. Admins can also dismiss a single run from the strip.
    "show_background_tasks": true
  },

  // Optional: recording to disk — motion/AI detection events (clips + thumbnails)
  // and, when switched on per camera in the web UI, continuous 24/7 footage.
  // Remove this section to disable. In Docker, mount a volume at "path".
  // "recording": {
  //   "path": "/recordings",           // storage directory
  //   "clips_path": "/clips",          // optional fast tier (SSD): new event clips land here,
  //                                    // 24/7 footage stays on "path"; unset = everything on "path"
  //   "archive_path": "/archive",      // optional cold tier: enables per-camera archiving in the
  //                                    // web UI — expired footage MOVES here instead of being
  //                                    // deleted. Use a different drive; in Docker, a second volume.
  //                                    // NOTE (Docker): map a volume at EVERY tier path you set —
  //                                    // an unmapped path is silently created inside the container
  //                                    // and that footage is lost when the container is recreated
  //   "retention_days": 7,             // delete events older than this (0 = keep forever)
  //   "pre_seconds": 5,                // video kept from before the detection
  //   "post_seconds": 8,               // quiet time before an event closes
  //   "max_clip_seconds": 120,         // hard cap per event/clip
  //   "stream": "auto",                // which stream to record: auto|mainStream|subStream
  //   "segment_minutes": 10,           // continuous recording: max length of one file
  //   "max_segment_size_mb": 256,      // continuous recording: max size of one file (rolls at whichever hits first)
  //   "continuous_retention_days": 7,  // continuous footage retention (default = retention_days)
  //   "encrypt": false                 // BETA: encrypt new footage at rest (AES-256-GCM). Playback and
  //                                    // old plaintext footage are unaffected. The key is NEOLINK_SECRET_KEY
  //                                    // or the state dir's secret.key — back it up (see README)
  // },

  // Optional: publish to MQTT for Home Assistant (auto-discovery). Remove to disable.
  // HA creates a device per camera with motion/person/vehicle/animal sensors,
  // battery, night-vision, floodlight, PIR, reboot + PTZ buttons and a snapshot,
  // depending on what each camera supports — plus a "Neolink.NET Server" device
  // with the server's own health (CPU, memory, disk, viewers, ...).
  // "mqtt": {
  //   "broker": "192.168.1.10",        // MQTT broker host (often the HA / Mosquitto box)
  //   "port": 1883,                    // 8883 with "tls": true
  //   "username": "neolink",
  //   "password": "CHANGE-ME",
  //   "client_id": "neolink",          // must be unique on the broker
  //   "base_topic": "neolink",         // root of the state/command topics
  //   "discovery": true,               // publish HA discovery config
  //   "discovery_prefix": "homeassistant",  // must match HA's MQTT integration
  //   "keepalive": 30,
  //   "tls": false,
  //   "stats_interval": 60             // server health publish cadence in seconds
  //                                    // (0 = off, minimum 5; editable in the web UI)
  // },

  // Optional: password-protect the RTSP server. Remove entries for open access.
  // Connect with e.g. rtsp://me:mepass@192.168.1.100:8654/driveway
  "users": [
    // { "name": "me", "pass": "mepass" }
  ],

  // One entry per camera.
  "cameras": [
    {
      // Name used in the RTSP URL and the web UI: rtsp://<host>:8654/driveway
      "name": "driveway",

      // Camera login (the same credentials as the Reolink app).
      "username": "admin",
      "password": "CHANGE-ME",

      // IP or hostname of the camera. Port defaults to 9000 if omitted.
      "address": "192.168.1.187:9000",

      // Optional: the camera's HTTP(S) web interface ("host", "host:port" or full URL).
      // Enables changing stream profiles (resolution/fps/bitrate) from the web UI;
      // most non-battery Reolink cameras serve it on port 80/443.
      // "http_address": "192.168.1.187",

      // Which streams to serve: "mainStream", "subStream", "externStream", "both", or "all".
      // "both" serves /driveway (+ /driveway/mainStream) and /driveway/subStream.
      "stream": "both",

      // Only needed when connecting through a Reolink NVR: camera channel, starting at 0.
      "channel_id": 0

      // Battery cameras (Argus etc.): unset = auto — cameras that report a battery
      // sleep while nobody watches (connecting on demand); everything else streams
      // around the clock. Set true to hold a battery camera awake 24/7 (needed for
      // recording/motion events; drains the battery — pair with solar/USB power),
      // or false to force sleep-friendly behavior.
      // "always_on": false,

      // UDP transport (EXPERIMENTAL) for battery-only models (parts of the Argus
      // line) that never listen on TCP — they log "Connection refused" forever on
      // the normal path because they speak Baichuan over UDP. Set the camera's UID
      // (Reolink app -> device info, or the sticker) and "udp": true to connect over
      // UDP instead. The camera must still be awake and reachable by "address" on
      // your LAN; a sleeping battery camera can't be woken over UDP either (PIR
      // motion or the Reolink app wakes it). Everything after connect — video,
      // events, battery, controls — works the same as the TCP path.
      // "uid": "95270000ABCDEFGH",
      // "udp": true,

      // Wake-capture (battery cameras): while sleep-friendly and unwatched, keep a
      // cheap liveness poll running so Neolink connects the moment the camera wakes
      // ITSELF (motion) and captures the event - instead of only connecting when a
      // viewer opens the stream. The poll doesn't reach a sleeping camera (its radio
      // is off), so it costs no battery, and it's silent in the log. This catches
      // events without holding the camera awake 24/7 like always_on. No effect with
      // always_on (never sleeps) or on non-battery cameras.
      // "wake_capture": true,

      // Optional: restrict this camera to specific users from "users" above.
      // "permitted_users": [ "me" ]
    }

    // Generic (non-Reolink) camera: give RTSP URLs instead of address/username —
    // put the login inside the URL. It streams, plays in the web UI and records
    // continuous (24/7) footage; Reolink-only extras (PTZ, LED, PIR, detection
    // events, stream profiles) are unavailable for these.
    // {
    //   "name": "garage-3rdparty",
    //   "rtsp_main": "rtsp://user:pass@192.168.1.60:554/stream1",   // required (or "rtsp")
    //   "rtsp_sub": "rtsp://user:pass@192.168.1.60:554/stream2"     // optional low-res twin
    // }
  ]
}
