bin/
obj/
.vs/
*.user
publish/

# Local test images exported with `docker save` (see the README's developer note)
/neolink.net-*.tar

# Real camera credentials live in config.json — never commit it.
# Copy config.example.json to config.json to get started.
config.json
src/Neolink.Server/config.json

# Editor/assistant local state + leftover pre-rename folder
.claude/
src/Neolink/

/src/Neolink.Server/users.json
/src/Neolink.Server/settings.json

# Notification subsystem runtime state — the AES key must NEVER be committed
# (it decrypts the stored SMTP password); notifications.json holds that encrypted
# secret. Ignored anywhere in the tree in case the server is run from the repo.
secret.key
notifications.json

# local docker test images (docker save output)
neolink-*.tar

# editor/backup leftovers
*.bak
storage-trend.json

# Local dev tools, not shipped
/devtools/
