# Changelog

The beta add-on tracks the `beta` branch and always reports its version as
`beta`, so Home Assistant never offers it as an update — reinstall it to pull a
fresh build.

Stable release notes:
https://github.com/borexola/neolink.net/blob/main/CHANGELOG.md
